@model IEnumerable<goodSamaritan.Models.Client.DuplicateFile>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsDuplicate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsDuplicate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DuplicateFileId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DuplicateFileId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DuplicateFileId })
        </td>
    </tr>
}

</table>
